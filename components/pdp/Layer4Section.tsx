import { Layer4 } from "@/components/home/Layer4";

/**
 * Layer4Section — PDP에서 홈의 4 LAYER 시연을 그대로 재사용 (RSC).
 *
 * 같은 카피·스펙 용어 사용. 홈과 PDP가 일치해야 신뢰감 유지.
 * 향후 카피/구조 분리가 필요하면 이 파일에서 분기.
 */
export function Layer4Section() {
  return <Layer4 />;
}
